<!-- arceus check-spec audit -->
> **Verdict (recorded by arceus)**: REQUEST_CHANGES
> **check-spec version**: check-spec v0.1.0-2-gbee34ec-dirty (commit bee34ec, built 2026-05-26T03:45:57Z)
> [!WARNING]
> [arceus] Audit report exceeds 2000 chars — this change may be too large; consider splitting via 'arceus change new'.
> Threshold: 2000 chars; this report: unknown.

# Spec/Code Consistency Audit — 2026-05-28-integrate-check-spec-as-completion-gate

_Integrate check-spec as completion gate_

- **Verdict**: REQUEST_CHANGES
- **Model**: claude-opus-4-7
- **Base → Head**: origin/main (0ae6f27) → HEAD (b8aaacb)
- **Files analyzed**: 26

## Summary

The diff implements the bulk of the check-spec completion gate feature as promised: ChangeMeta extensions (T-1/T-2), config schema (T-3), src/integrations/check-spec.ts with error classification (T-4/T-12/T-13/T-14), three-mode gate in updateChangeStatus with zero-commit defense and force handling (T-5), `change verify` CLI subcommand with disabled branch, audit report persistence, 2000-char warning, metadata header (T-6/T-7/T-8), `change status --force --reason` (T-9), `change show` Audit block (T-10), `arceus init` check-spec tip (T-11), skill/CLAUDE.md/architecture doc updates (T-15/T-16/T-17), and unit tests for the gate and the integration (T-18/T-19/T-20). However, the diff also bundles work from a sibling change (gitignore handling) that is out of scope per this change's spec, and most critically the committed audit/latest.md shows the change itself received a REQUEST_CHANGES verdict from check-spec (oversize report, AC14 explicitly fails), and T-25 is correctly left unchecked. Self-reported AC14 cannot pass given the committed audit artifact. Strict-mode `--force` warning text in src/state/changes.ts does not exactly match F3 (`[arceus] Strict gate bypassed via --force` vs the spec's prescribed wording is not explicitly fixed, acceptable). The audit header reports `this report: unknown` rather than the actual length (minor drift from spec F1 intent but not explicitly required).

## Task implementation (from tasks.md)

| # | Phase | Task | Reported | Actual | Evidence |
|---|-------|------|----------|--------|----------|
| 1 | Phase 1 — Types & state layer | T-1 在 `src/state/changes.ts` 的 `ChangeMeta` 介面新增 `verdict` / `verifiedSha` / `verifiedAt` / `verifiedBase` / `verificationModel` 五個 optional 欄位（含 JSDoc）— 也加了 `verificationBinaryVersion` 第六個欄位 | [x] | done | src/state/changes.ts:32-44 adds verdict/verifiedSha/verifiedAt/verifiedBase/verificationModel/verificationBinaryVersion |
| 2 | Phase 1 — Types & state layer | T-2 在 `src/state/changes.ts` 新增 `getAuditDir(arceusDir, id)` / `getAuditLatestPath(arceusDir, id)` 兩個 path helper — 也加了 `getForceOverridesLogPath` | [x] | done | src/state/changes.ts:94-106 getAuditDir/getAuditLatestPath/getForceOverridesLogPath |
| 3 | Phase 1 — Types & state layer | T-3 在 `src/state/config.ts` 的 config schema 加 `checkSpec?: { enabled: boolean; binary: string; requireApprove: boolean }`，預設值 `{ enabled: true, binary: "check-spec", requireApprove: false }`（advisory 模式為預設） | [x] | done | src/state/config.ts:22-33 checkSpec schema added |
| 4 | Phase 1 — Types & state layer | T-4 新增 `src/integrations/check-spec.ts`：`runCheckSpec` + `findBinary` + `isReportOversize` + 錯誤分類（binary-not-found / missing-api-key / git-ref-not-found / parse-failed / binary-crash） | [x] | done | src/integrations/check-spec.ts (new file, 261 lines) with findBinary, runCheckSpec, isReportOversize, error kinds |
| 5 | Phase 1 — Types & state layer | T-5 修改 `updateChangeStatus()`：依 F3 表格實作三態 + zero-commit 防衛 + `--force` 處理 + force-overrides.log 寫入 | [x] | done | src/state/changes.ts enforceCompletionGate + resolveHeadSha + writeForceOverride |
| 6 | Phase 2 — CLI surface | T-6 在 `src/cli.ts` 新增 `change verify <id>` 子命令含全部 flag，含 disabled 分支 | [x] | done | src/cli.ts change verify subcommand with --base/--head/--format/--model/--no-save; disabled branch handled |
| 7 | Phase 2 — CLI surface | T-7 markdown 報告寫入 `audit/<ts>.md` + `audit/latest.md` + meta.json 寫回（透過 `recordVerification`）+ 報告開頭 metadata header | [x] | done | src/cli.ts persistReport writes audit/<ts>.ext + latest.md; recordVerification persists to meta.json; renderAuditHeader writes header |
| 8 | Phase 2 — CLI surface | T-8 2000 字警告：stderr 印 + audit 檔開頭插 `> [!WARNING]` 區塊；退出碼不變 | [x] | partial | src/cli.ts uses OVERSIZE_WARNING_MESSAGE on stderr and renderAuditHeader inserts > [!WARNING]; however header says 'this report: unknown' instead of actual size — minor drift from spec intent but functionally satisfies AC11 |
| 9 | Phase 2 — CLI surface | T-9 `change status` 加 `--force` flag + `--reason`：strict 跳過閘門 + 寫 log；advisory/disabled 印 no-op 訊息 | [x] | done | src/cli.ts change status command gains --force and --reason options |
| 10 | Phase 2 — CLI surface | T-10 `change show <id>` 加 Audit 區塊（verdict 存在時才印） | [x] | done | src/cli.ts change show prints Audit block when c.verdict exists |
| 11 | Phase 2 — CLI surface | T-11 `arceus init` first-run tip：偵測 PATH（平台分支已抽到 `findBinary` 內），命中印 `✓ ... detected`、否則印安裝指引；upgrade 路徑也印 | [x] | done | src/cli.ts printCheckSpecTip; called from both init paths |
| 12 | Phase 3 — Error handling | T-12 binary 不存在：`findBinary` 預先 lookup + `errorKind: "binary-not-found"` + 安裝指引訊息 + exit code 2 | [x] | done | src/integrations/check-spec.ts findBinary + binary-not-found errorKind with install instructions |
| 13 | Phase 3 — Error handling | T-13 `ANTHROPIC_API_KEY`：從 check-spec stderr 偵測 + 安裝指引 + exit code 2 | [x] | done | src/integrations/check-spec.ts classifyError detects ANTHROPIC_API_KEY in stderr |
| 14 | Phase 3 — Error handling | T-14 非 0/1 退出碼或 parse 失敗：`errorKind: "binary-crash" / "parse-failed"`，audit 檔保留原 stdout，CLI 印警告 | [x] | done | src/integrations/check-spec.ts binary-crash and parse-failed branches preserve stdout |
| 15 | Phase 4 — Skill / docs 更新 | T-15 `skills/apply/SKILL.md`：插入 Step 5.5 (Audit via check-spec)，含三種 verdict branching + 3 輪 re-verify + advisory/strict 模式差異 | [x] | done | skills/apply/SKILL.md adds Step 5.5 with verdict branching, 3-round limit, advisory/strict notes |
| 16 | Phase 4 — Skill / docs 更新 | T-16 `CLAUDE.md` "Evidence-Driven Verification"：補充兩層驗證設計 + 三態 gate 表格 + audit size heuristic + 報告儲存 | [x] | done | CLAUDE.md Evidence-Driven Verification section adds two-layer design, three-state table, audit-size heuristic, report storage |
| 17 | Phase 4 — Skill / docs 更新 | T-17 `docs/architecture/arceus-plugin-architecture.md`：加 9.1「外部稽核：check-spec 整合」段含資料流向 + 三態表 | [x] | done | docs/architecture/arceus-plugin-architecture.md adds section 9.1 with data-flow diagram and three-state table |
| 18 | Phase 5 — Tests | T-18 `tests/unit/state/changes-gate.test.ts` 含 disabled / advisory / strict × {verdict 缺、verdict 非 APPROVE、SHA 不符、APPROVE 過} + zero-commit case + force-overrides.log 寫入 + recordVerification 持久化 | [x] | done | tests/unit/state/changes-gate.test.ts covers disabled/advisory/strict, zero-commit, force overrides, recordVerification |
| 19 | Phase 5 — Tests | T-19 `tests/unit/integrations/check-spec.test.ts` 用 shell-script fake binary 測 happy path + REQUEST_CHANGES + binary-not-found + missing-api-key + git-error + crash + version-fail + oversize + findBinary + isReportOversize | [x] | done | tests/unit/integrations/check-spec.test.ts covers happy path, REQUEST_CHANGES, binary-not-found, missing-api-key, git-error, crash, version-fail, oversize |
| 20 | Phase 5 — Tests | T-20 2000 字警告：fake binary `oversize` mode 觸發 + assert `isReportOversize(result.report) === true`（CLI 層的 stderr 警告由現有 e2e dogfood 證實） | [x] | partial | check-spec.test.ts asserts isReportOversize on oversize; CLI-layer stderr warning + header WARNING block is not asserted in unit tests (relies on dogfood) |
| 21 | Phase 5 — Tests | T-21 init tip 測試：在 e2e CLI dogfood 中驗證（`node dist/cli.js init` 印出 detected 或 install tip）；本機 PATH 上有 check-spec，已實際觀察到 `✓ check-spec detected at ...` | [x] | partial | No automated test for init tip; relies on manual smoke test per task note |
| 22 | Phase 5 — Tests | T-22 完整流程覆蓋：advisory 通過 + strict 通過 + strict 被擋（verdict）+ strict 被擋（SHA）+ strict force + advisory force no-op — 全部由 changes-gate.test.ts 涵蓋 | [x] | done | changes-gate.test.ts covers advisory pass, strict pass, strict blocked by verdict, strict blocked by SHA, strict force, advisory force no-op |
| 23 | Phase 5 — Tests | T-23 disabled 分支已實作於 CLI verify 中（spec.md F4），unit test 在 changes-gate 的 disabled describe 中覆蓋；e2e 由 CLI 手動 smoke test 證實 | [x] | partial | disabled unit test exists in changes-gate.test.ts but disabled-mode verify CLI flow (writing audit, skipping meta.json) has no automated test — relies on smoke test |
| 24 | Phase 5 — Tests | T-24 跑 `npm run verify`：typecheck + lint + 69 tests passed (6 files) + build 全綠 | [x] | done | tasks.md reports 69 tests passing; cannot independently rerun in audit |
| 25 | Phase 6 — Self-verification (eat own dog food) | T-25 把這個 change 本身用新建好的 `arceus change verify` 跑一次，取得 APPROVE，且 audit 報告字數 < 2000 字後再 `change status completed`（AC14，最有意義的 demo）。若報告 ≥ 2000 字，**不**強行通過——回頭把本 change 拆分成多個小 change 再重做 | [ ] | missing | .arceus/changes/<id>/audit/latest.md committed in the diff shows verdict=REQUEST_CHANGES and oversize warning; AC14 not met |
|   |   |   |   | **notes** | Tasks.md correctly leaves T-25 unchecked, but this means the change's own dogfood demo fails — AC14 explicitly required APPROVE + <2000 chars |

## Acceptance criteria (from spec.md)

- **PASS**: criterion 1 — **AC1**: 在乾淨的 sandbox 跑 `node dist/cli.js change verify <id>`（給定 mock binary 回傳 APPROVE JSON），meta.json 出現 `verdict: "APPROVE"` 且 `verifiedSha` 等於 `git rev-parse HEAD`
  - evidence: src/cli.ts change verify writes verdict + verifiedSha (from git rev-parse HEAD) via recordVerification
- **PASS**: criterion 2 — **AC2 (advisory, default)**: 在預設 config (`requireApprove: false`) 下，`change status <id> completed` 即使 verdict 缺失也成功，但 stderr 必須出現 F3 規定的 advisory 警告訊息
  - evidence: src/state/changes.ts enforceCompletionGate advisory branch emits warning, does not throw
- **PASS**: criterion 3 — **AC3 (strict, opt-in)**: 把 config 改成 `requireApprove: true` 後，verdict 缺失時 `change status completed` 失敗，error message 提到要先跑 verify
  - evidence: strict branch throws 'No APPROVE verdict for ... Run arceus change verify ...'
- **PASS**: criterion 4 — **AC4 (SHA freshness)**: strict 模式下，verdict 為 APPROVE 但 `verifiedSha` 與當前 HEAD 不符時（模擬 verify 後又 commit），status 轉移仍被擋下
  - evidence: strict branch throws 'verifiedSha does not match current HEAD'
- **PASS**: criterion 5 — **AC5 (force)**: strict 模式下，`--force` 確實能跳過閘門，且 `audit/force-overrides.log` 有一筆紀錄
  - evidence: writeForceOverride appends to audit/force-overrides.log; changes-gate.test.ts asserts content
- **PASS**: criterion 6 — **AC6 (disabled)**: `config.checkSpec.enabled = false` 時，整個閘門被旁路（不擋、也不寫 verdict 進 meta.json）
  - evidence: disabled branch in updateChangeStatus returns early; cli verify's disabled branch skips recordVerification
- **PASS**: criterion 7 — **AC7 (errors)**: 缺 binary / 缺 API key 時，錯誤訊息符合「錯誤處理」段落規範，**不**出現 stack trace
  - evidence: src/integrations/check-spec.ts returns structured errorKind+errorMessage with actionable text; cli.ts surfaces and exits 2
- **PASS**: criterion 8 — **AC8 (show)**: `change show <id>` 在 verdict 存在時顯示 Audit 區塊
  - evidence: src/cli.ts change show prints Audit block guarded by if (c.verdict)
- **PASS**: criterion 9 — **AC9 (skill)**: `skills/apply/SKILL.md` 中存在 Step 5.5，且 Step 6 仍維持原 `change status completed` 呼叫
  - evidence: skills/apply/SKILL.md Step 5.5 inserted; Step 6 still calls `npx arceus change status <id> completed`
- **PASS**: criterion 10 — **AC10 (archive)**: 既有 `.arceus/changes/` 下尚未有 verdict 的 changes，能正常被 archive（archive 路徑不受閘門影響）
  - evidence: archive path unchanged in updateChangeStatus / archiveChange; gate only checks completed transitions
- **PARTIAL**: criterion 11 — **AC11 (size warning)**: 給定 mock binary 回傳一份 >2000 字的 markdown 報告，verify 完成後 stderr 出現 F1 規定的「change may be too large」警告，且 audit 檔開頭有 `> [!WARNING]` 區塊；退出碼**不**因警告而改變
  - evidence: src/cli.ts emits OVERSIZE_WARNING_MESSAGE to stderr and header includes > [!WARNING]; however 'this report: unknown' literal in renderAuditHeader is a placeholder rather than actual length — does not strictly invalidate AC11 but is a minor drift
- **PASS**: criterion 12 — **AC12 (init tip)**: `arceus init` 印出 F7 規定的 check-spec 安裝指引；當 PATH 上已有 check-spec 時改顯示 `✓ check-spec detected at <path>`
  - evidence: printCheckSpecTip prints install tip; when findBinary returns a path it prints detected message
- **PASS**: criterion 13 — **AC13 (verify)**: `npm run verify` (typecheck + lint + test + build) 全綠
  - evidence: tasks.md claims 69 tests passing; build artifacts not in diff but no evidence of failure
- **FAIL**: criterion 14 — **AC14 (dogfood)**: 本 change 自身在 apply 完成後，能跑過 `check-spec verify` 並取得 APPROVE（吃自己的狗食，且 audit 報告字數 < 2000 字——若超過，代表此 change 本身就違反自己訂的規矩，需要拆分）
  - evidence: .arceus/changes/<id>/audit/latest.md committed in diff shows REQUEST_CHANGES verdict and oversize warning — dogfood demonstrably failed
  - notes: AC14 explicitly requires APPROVE and <2000 chars; the committed audit shows the opposite outcome
- **PASS**: criterion 15 — **AC15 (zero-commit)**: 在 zero-commit repo（`git init` 但無任何 commit）跑 strict 模式的 `change status <id> completed`，必須印 F3 規定的「Cannot resolve HEAD」訊息、退出碼 2，**不**拋出原始 git stderr stack
  - evidence: resolveHeadSha distinguishes zero-commit case and gate throws 'Cannot resolve HEAD ...'; changes-gate.test.ts covers it
- **PASS**: criterion 16 — **AC16 (force in advisory)**: advisory 模式下執行 `change status <id> completed --force` 必須印 F3 規定的「--force has no effect in advisory mode」訊息且**不**寫 force-overrides.log
  - evidence: advisory branch logs '--force has no effect in advisory mode' and skips writeForceOverride
- **PASS**: criterion 17 — **AC17 (disabled verify)**: `config.checkSpec.enabled = false` 下跑 `change verify <id>`，stderr 出現 F4 規定的「report saved but verdict not recorded」訊息，audit 檔有被建立，但 meta.json 的 verdict 欄位仍為 undefined
  - evidence: src/cli.ts disabled branch in change verify writes audit via persistReport and prints 'report saved but verdict not recorded' to stderr; does not call recordVerification
- **PASS**: criterion 18 — **AC18 (version recorded)**: audit 報告 metadata 開頭含 check-spec 版本字串；mock binary 的 `version` 子命令失敗時，版本欄位顯示 `unknown` 但 verify 流程不失敗
  - evidence: renderAuditHeader emits '> **check-spec version**: ${result.binaryVersion}'; getBinaryVersion returns 'unknown' on failure

## Drift findings

**Undocumented additions** (in diff, not in spec):

- Sibling change's gitignore work bundled in this diff: .gitignore, .arceus/.gitignore, .arceus/.gitkeep, ensureArceusGitignore in src/state/config.ts, CLAUDE.md 'which files commit' section, tests/unit/state/gitignore.test.ts, init wiring in src/cli.ts
- Planning artifacts for sibling change committed in this diff: .arceus/changes/2026-05-28-track-changes-folder-and-config-in-git/*
- Committed audit artifacts (audit/latest.md, audit/2026-05-28T08-20-17-798Z.md) which transitively expose a REQUEST_CHANGES verdict on this very change
- src/cli.ts imports writeFileSync, copyFileSync, mkdirSync, spawnSync — fine, but readFileSync import retained though unused in changed paths (minor)

**Missing from implementation** (in spec, not in diff):

- AC14 dogfood: change cannot ship marked completed with the committed REQUEST_CHANGES audit
- Audit header oversize line uses placeholder 'this report: unknown' instead of actual character count (minor drift from spec F1 advisory text)
- Automated tests for CLI-layer stderr 2000-char warning (T-20 CLI assertion) and init tip detection (T-21) — implementer acknowledges these are covered by manual smoke tests only

## Open questions for human reviewer

- Was the sibling-change work (gitignore + planning artifacts for 2026-05-28-track-changes-folder-and-config-in-git) intentionally bundled into this change's PR, or should it be split into a separate commit/PR per Decision 9's 'one change one concern' principle?
- Given the committed audit/latest.md reports REQUEST_CHANGES for this very change, is the change actually ready to be marked completed, or is the human reviewer expected to address the dogfood failure (AC14) before merge?
- Should the renderAuditHeader compute and substitute the actual report length instead of the 'unknown' placeholder, to fully honor spec.md F1's intent?

