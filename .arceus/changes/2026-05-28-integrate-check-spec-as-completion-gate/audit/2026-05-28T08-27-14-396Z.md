<!-- arceus check-spec audit -->
> **Verdict (recorded by arceus)**: REQUEST_CHANGES
> **check-spec version**: check-spec v0.1.0-2-gbee34ec-dirty (commit bee34ec, built 2026-05-26T03:45:57Z)
> [!WARNING]
> [arceus] Audit report exceeds 2000 chars — this change may be too large; consider splitting via 'arceus change new'.
> Threshold: 2000 chars; this report: 14503 chars.

# Spec/Code Consistency Audit — 2026-05-28-integrate-check-spec-as-completion-gate

_Integrate check-spec as completion gate_

- **Verdict**: REQUEST_CHANGES
- **Model**: claude-opus-4-7
- **Base → Head**: origin/main (0ae6f27) → HEAD (f1290f4)
- **Files analyzed**: 26

## Summary

The implementation substantively delivers the check-spec completion gate as specified: ChangeMeta extension (T-1), audit path helpers (T-2), config schema (T-3), the integrations module (T-4), three-mode gate logic with zero-commit defense and force handling (T-5), the `change verify` CLI subcommand with all flags including disabled branch (T-6/T-7), 2000-char oversize warning (T-8), `change status --force/--reason` (T-9), `change show` Audit block (T-10), init tip (T-11), error classification (T-12/T-13/T-14), apply skill Step 5.5 (T-15), CLAUDE.md + architecture doc updates (T-16/T-17), and substantial test coverage (T-18/T-19). Self-verification (T-25) is honestly left unchecked. However, AC14 (dogfood) explicitly fails — meta.json shows verdict=REQUEST_CHANGES and the included audit/latest.md is ~104 lines >2000 chars listing 'all tasks missing' — the recorded audit was run against a wrong base (`0ae6f27`→`e479c7b`) before the implementation was committed and was never re-run. The diff also contains the sibling change's planning + implementation (gitignore/ensureArceusGitignore work), which is undocumented drift in this change's scope.

## Task implementation (from tasks.md)

| # | Phase | Task | Reported | Actual | Evidence |
|---|-------|------|----------|--------|----------|
| 1 | Phase 1 — Types & state layer | T-1 在 `src/state/changes.ts` 的 `ChangeMeta` 介面新增 `verdict` / `verifiedSha` / `verifiedAt` / `verifiedBase` / `verificationModel` 五個 optional 欄位（含 JSDoc）— 也加了 `verificationBinaryVersion` 第六個欄位 | [x] | done | src/state/changes.ts:33-44 |
| 2 | Phase 1 — Types & state layer | T-2 在 `src/state/changes.ts` 新增 `getAuditDir(arceusDir, id)` / `getAuditLatestPath(arceusDir, id)` 兩個 path helper — 也加了 `getForceOverridesLogPath` | [x] | done | src/state/changes.ts:97-107 |
| 3 | Phase 1 — Types & state layer | T-3 在 `src/state/config.ts` 的 config schema 加 `checkSpec?: { enabled: boolean; binary: string; requireApprove: boolean }`，預設值 `{ enabled: true, binary: "check-spec", requireApprove: false }`（advisory 模式為預設） | [x] | done | src/state/config.ts:22-34 |
| 4 | Phase 1 — Types & state layer | T-4 新增 `src/integrations/check-spec.ts`：`runCheckSpec` + `findBinary` + `isReportOversize` + 錯誤分類（binary-not-found / missing-api-key / git-ref-not-found / parse-failed / binary-crash） | [x] | done | src/integrations/check-spec.ts:1-261 |
| 5 | Phase 1 — Types & state layer | T-5 修改 `updateChangeStatus()`：依 F3 表格實作三態 + zero-commit 防衛 + `--force` 處理 + force-overrides.log 寫入 | [x] | done | src/state/changes.ts:415-470 + enforceCompletionGate |
| 6 | Phase 2 — CLI surface | T-6 在 `src/cli.ts` 新增 `change verify <id>` 子命令含全部 flag，含 disabled 分支 | [x] | done | src/cli.ts:255-340 |
| 7 | Phase 2 — CLI surface | T-7 markdown 報告寫入 `audit/<ts>.md` + `audit/latest.md` + meta.json 寫回（透過 `recordVerification`）+ 報告開頭 metadata header | [x] | done | src/cli.ts:persistReport+renderAuditHeader+recordVerification call |
| 8 | Phase 2 — CLI surface | T-8 2000 字警告：stderr 印 + audit 檔開頭插 `> [!WARNING]` 區塊；退出碼不變 | [x] | done | src/cli.ts oversize branch + renderAuditHeader WARNING block |
| 9 | Phase 2 — CLI surface | T-9 `change status` 加 `--force` flag + `--reason`：strict 跳過閘門 + 寫 log；advisory/disabled 印 no-op 訊息 | [x] | done | src/cli.ts:235-252 (--force/--reason on status) |
| 10 | Phase 2 — CLI surface | T-10 `change show <id>` 加 Audit 區塊（verdict 存在時才印） | [x] | done | src/cli.ts:213-227 (Audit block in show) |
| 11 | Phase 2 — CLI surface | T-11 `arceus init` first-run tip：偵測 PATH（平台分支已抽到 `findBinary` 內），命中印 `✓ ... detected`、否則印安裝指引；upgrade 路徑也印 | [x] | done | src/cli.ts:printCheckSpecTip + init wiring |
| 12 | Phase 3 — Error handling | T-12 binary 不存在：`findBinary` 預先 lookup + `errorKind: "binary-not-found"` + 安裝指引訊息 + exit code 2 | [x] | done | src/integrations/check-spec.ts findBinary + binary-not-found branch |
| 13 | Phase 3 — Error handling | T-13 `ANTHROPIC_API_KEY`：從 check-spec stderr 偵測 + 安裝指引 + exit code 2 | [x] | done | src/integrations/check-spec.ts classifyError missing-api-key |
| 14 | Phase 3 — Error handling | T-14 非 0/1 退出碼或 parse 失敗：`errorKind: "binary-crash" / "parse-failed"`，audit 檔保留原 stdout，CLI 印警告 | [x] | done | src/integrations/check-spec.ts binary-crash/parse-failed branches |
| 15 | Phase 4 — Skill / docs 更新 | T-15 `skills/apply/SKILL.md`：插入 Step 5.5 (Audit via check-spec)，含三種 verdict branching + 3 輪 re-verify + advisory/strict 模式差異 | [x] | done | skills/apply/SKILL.md Step 5.5 |
| 16 | Phase 4 — Skill / docs 更新 | T-16 `CLAUDE.md` "Evidence-Driven Verification"：補充兩層驗證設計 + 三態 gate 表格 + audit size heuristic + 報告儲存 | [x] | done | CLAUDE.md Evidence-Driven Verification section |
| 17 | Phase 4 — Skill / docs 更新 | T-17 `docs/architecture/arceus-plugin-architecture.md`：加 9.1「外部稽核：check-spec 整合」段含資料流向 + 三態表 | [x] | done | docs/architecture/arceus-plugin-architecture.md section 9.1 |
| 18 | Phase 5 — Tests | T-18 `tests/unit/state/changes-gate.test.ts` 含 disabled / advisory / strict × {verdict 缺、verdict 非 APPROVE、SHA 不符、APPROVE 過} + zero-commit case + force-overrides.log 寫入 + recordVerification 持久化 | [x] | done | tests/unit/state/changes-gate.test.ts (full matrix incl. zero-commit + force log) |
| 19 | Phase 5 — Tests | T-19 `tests/unit/integrations/check-spec.test.ts` 用 shell-script fake binary 測 happy path + REQUEST_CHANGES + binary-not-found + missing-api-key + git-error + crash + version-fail + oversize + findBinary + isReportOversize | [x] | done | tests/unit/integrations/check-spec.test.ts |
| 20 | Phase 5 — Tests | T-20 2000 字警告：fake binary `oversize` mode 觸發 + assert `isReportOversize(result.report) === true`（CLI 層的 stderr 警告由現有 e2e dogfood 證實） | [x] | partial | tests/unit/integrations/check-spec.test.ts oversize block |
|   |   |   |   | **notes** | isReportOversize asserted; CLI-layer stderr warning is not unit-tested as the task notes, relying on e2e |
| 21 | Phase 5 — Tests | T-21 init tip 測試：在 e2e CLI dogfood 中驗證（`node dist/cli.js init` 印出 detected 或 install tip）；本機 PATH 上有 check-spec，已實際觀察到 `✓ check-spec detected at ...` | [x] | partial | src/cli.ts printCheckSpecTip exists but no test |
|   |   |   |   | **notes** | No automated test added; relies on manual observation per task notes |
| 22 | Phase 5 — Tests | T-22 完整流程覆蓋：advisory 通過 + strict 通過 + strict 被擋（verdict）+ strict 被擋（SHA）+ strict force + advisory force no-op — 全部由 changes-gate.test.ts 涵蓋 | [x] | done | changes-gate.test.ts covers six scenarios |
| 23 | Phase 5 — Tests | T-23 disabled 分支已實作於 CLI verify 中（spec.md F4），unit test 在 changes-gate 的 disabled describe 中覆蓋；e2e 由 CLI 手動 smoke test 證實 | [x] | partial | src/cli.ts disabled branch, changes-gate.test.ts disabled describe |
|   |   |   |   | **notes** | Disabled branch present in CLI verify and gate logic; disabled gate has a unit test, but disabled-verify (the F4 stderr message + skipping meta write in CLI) has no automated test |
| 24 | Phase 5 — Tests | T-24 跑 `npm run verify`：typecheck + lint + 69 tests passed (6 files) + build 全綠 | [x] | done | tasks.md self-report |
|   |   |   |   | **notes** | Self-reported 69 tests passing; cannot independently verify but no contrary evidence |
| 25 | Phase 6 — Self-verification (eat own dog food) | T-25 把這個 change 本身用新建好的 `arceus change verify` 跑一次，取得 APPROVE，且 audit 報告字數 < 2000 字後再 `change status completed`（AC14，最有意義的 demo）。若報告 ≥ 2000 字，**不**強行通過——回頭把本 change 拆分成多個小 change 再重做 | [ ] | missing | meta.json verdict=REQUEST_CHANGES; audit/latest.md |
|   |   |   |   | **notes** | Honestly unchecked; meta.json records REQUEST_CHANGES verdict and oversized report — dogfood explicitly failed |

## Acceptance criteria (from spec.md)

- **PASS**: criterion 1 — **AC1**: 在乾淨的 sandbox 跑 `node dist/cli.js change verify <id>`（給定 mock binary 回傳 APPROVE JSON），meta.json 出現 `verdict: "APPROVE"` 且 `verifiedSha` 等於 `git rev-parse HEAD`
  - evidence: src/cli.ts verify command + recordVerification writes verdict+verifiedSha; covered by recordVerification test
- **PASS**: criterion 2 — **AC2 (advisory, default)**: 在預設 config (`requireApprove: false`) 下，`change status <id> completed` 即使 verdict 缺失也成功，但 stderr 必須出現 F3 規定的 advisory 警告訊息
  - evidence: changes-gate.test.ts advisory describe; warn message matches F3
- **PASS**: criterion 3 — **AC3 (strict, opt-in)**: 把 config 改成 `requireApprove: true` 後，verdict 缺失時 `change status completed` 失敗，error message 提到要先跑 verify
  - evidence: changes-gate.test.ts strict describe (verdict missing → throw with verify hint)
- **PASS**: criterion 4 — **AC4 (SHA freshness)**: strict 模式下，verdict 為 APPROVE 但 `verifiedSha` 與當前 HEAD 不符時（模擬 verify 後又 commit），status 轉移仍被擋下
  - evidence: changes-gate.test.ts 'SHA does not match HEAD' test
- **PASS**: criterion 5 — **AC5 (force)**: strict 模式下，`--force` 確實能跳過閘門，且 `audit/force-overrides.log` 有一筆紀錄
  - evidence: changes-gate.test.ts '--force bypasses and writes audit/force-overrides.log'
- **PASS**: criterion 6 — **AC6 (disabled)**: `config.checkSpec.enabled = false` 時，整個閘門被旁路（不擋、也不寫 verdict 進 meta.json）
  - evidence: changes-gate.test.ts disabled describe; src/cli.ts disabled branch in verify skips meta write
- **PASS**: criterion 7 — **AC7 (errors)**: 缺 binary / 缺 API key 時，錯誤訊息符合「錯誤處理」段落規範，**不**出現 stack trace
  - evidence: check-spec.ts error branches return errorKind+errorMessage without throwing; CLI prints message and exits 2
- **PASS**: criterion 8 — **AC8 (show)**: `change show <id>` 在 verdict 存在時顯示 Audit 區塊
  - evidence: src/cli.ts:213-227 in show subcommand
- **PASS**: criterion 9 — **AC9 (skill)**: `skills/apply/SKILL.md` 中存在 Step 5.5，且 Step 6 仍維持原 `change status completed` 呼叫
  - evidence: skills/apply/SKILL.md Step 5.5 inserted; Step 6 retains change status completed call
- **PASS**: criterion 10 — **AC10 (archive)**: 既有 `.arceus/changes/` 下尚未有 verdict 的 changes，能正常被 archive（archive 路徑不受閘門影響）
  - evidence: archiveChange unchanged; gate only triggers on status==='completed'
- **PARTIAL**: criterion 11 — **AC11 (size warning)**: 給定 mock binary 回傳一份 >2000 字的 markdown 報告，verify 完成後 stderr 出現 F1 規定的「change may be too large」警告，且 audit 檔開頭有 `> [!WARNING]` 區塊；退出碼**不**因警告而改變
  - evidence: src/cli.ts oversize handling + check-spec.test.ts oversize
  - notes: isReportOversize tested at unit level; CLI-layer stderr emit + audit WARNING block exists in code (renderAuditHeader) but no end-to-end automated assertion
- **PARTIAL**: criterion 12 — **AC12 (init tip)**: `arceus init` 印出 F7 規定的 check-spec 安裝指引；當 PATH 上已有 check-spec 時改顯示 `✓ check-spec detected at <path>`
  - evidence: src/cli.ts:88-101
  - notes: Implementation present (printCheckSpecTip with findBinary detection); no automated test, task self-reports manual observation only
- **PASS**: criterion 13 — **AC13 (verify)**: `npm run verify` (typecheck + lint + test + build) 全綠
  - evidence: tasks.md T-24
  - notes: Self-reported 69 tests passing; cannot independently verify
- **FAIL**: criterion 14 — **AC14 (dogfood)**: 本 change 自身在 apply 完成後，能跑過 `check-spec verify` 並取得 APPROVE（吃自己的狗食，且 audit 報告字數 < 2000 字——若超過，代表此 change 本身就違反自己訂的規矩，需要拆分）
  - evidence: meta.json verdict, audit/latest.md content
  - notes: meta.json explicitly records verdict=REQUEST_CHANGES; audit/latest.md is >2000 chars and lists all tasks as missing (because the audit was run against a base/head pair that pre-dated the implementation). Decision 10 explicitly acknowledges dogfood failure and chooses to ship in advisory anyway, but AC14's literal requirement (APPROVE + <2000 chars) is not met
- **PASS**: criterion 15 — **AC15 (zero-commit)**: 在 zero-commit repo（`git init` 但無任何 commit）跑 strict 模式的 `change status <id> completed`，必須印 F3 規定的「Cannot resolve HEAD」訊息、退出碼 2，**不**拋出原始 git stderr stack
  - evidence: changes-gate.test.ts 'strict gate on zero-commit repo' describe
- **PASS**: criterion 16 — **AC16 (force in advisory)**: advisory 模式下執行 `change status <id> completed --force` 必須印 F3 規定的「--force has no effect in advisory mode」訊息且**不**寫 force-overrides.log
  - evidence: changes-gate.test.ts advisory '--force has no effect' test + 'does NOT write force-overrides.log' test
- **PARTIAL**: criterion 17 — **AC17 (disabled verify)**: `config.checkSpec.enabled = false` 下跑 `change verify <id>`，stderr 出現 F4 規定的「report saved but verdict not recorded」訊息，audit 檔有被建立，但 meta.json 的 verdict 欄位仍為 undefined
  - evidence: src/cli.ts disabled branch in verify; no test in changes-gate.test.ts covers CLI verify disabled output
  - notes: Disabled-verify behavior in CLI (stderr message + skip meta write) lacks an explicit automated test; only the gate-side disabled branch is unit-tested
- **PASS**: criterion 18 — **AC18 (version recorded)**: audit 報告 metadata 開頭含 check-spec 版本字串；mock binary 的 `version` 子命令失敗時，版本欄位顯示 `unknown` 但 verify 流程不失敗
  - evidence: check-spec.test.ts 'version-fail' test + renderAuditHeader writes binaryVersion line

## Drift findings

**Undocumented additions** (in diff, not in spec):

- Sibling change's gitignore work (root .gitignore, .arceus/.gitignore, .arceus/.gitkeep, ensureArceusGitignore in src/state/config.ts, CLAUDE.md 'which files commit' section, tests/unit/state/gitignore.test.ts, src/cli.ts init wiring) is bundled into this diff but belongs to 2026-05-28-track-changes-folder-and-config-in-git
- Planning artifacts for the sibling change (.arceus/changes/2026-05-28-track-changes-folder-and-config-in-git/*) are included

**Missing from implementation** (in spec, not in diff):

- T-25 dogfood: no successful APPROVE+<2000-char audit run was produced for this change itself

## Open questions for human reviewer

- Decision 10 explicitly accepts shipping in advisory mode despite dogfood failure (REQUEST_CHANGES + oversized report). Does the reviewer accept that AC14 is being intentionally waived, or does the dogfood need to be re-run after committing the implementation (the current audit was against base→head e479c7b which appears to predate this implementation commit f1290f4)?
- Should the sibling gitignore change's contents be excluded from this PR/diff, or is bundling them acceptable because this change depended on the gitignore fix to commit artifacts?
- AC11/AC12/AC17 lack explicit automated tests despite tasks T-20/T-21/T-23 being checked — is the documented reliance on manual e2e observation acceptable?

